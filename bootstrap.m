function P_0, X_0 = bootstrap(img0, img1)
% BOOTSTRAP returns the pointcould and keypoint image coordinates from two
% initialization images, img0 and img1
% Returns
% P_0, a 2*M matrix of image coordinates on img0
% X_0, a 3*M matrix of 3D positions, the triangulated poincloud
end